# Copy one of these into a real env file:
#   .env.development -> used for local/dev runs (NODE_ENV=development)
#   .env.production  -> used for built/prod runs (NODE_ENV=production)
#
# The bot loads .env.${NODE_ENV}.
# Development defaults to .env.development when NODE_ENV is unset.
# Production loads .env.production.
